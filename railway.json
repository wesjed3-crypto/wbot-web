{
  "build": {
    "command": "npm install --production && npm cache clean --force",
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node web.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "always",
    "numReplicas": 1,
    "sleepApplication": false
  }
}
